#include "iirfilter.h"

/**
 * Main method required in all C programs. Simply calls filter with a predefined string length
 *
 * @param void
 * @return int 0
 */

int main() {
	filter(0.65);
	return 0;
}
